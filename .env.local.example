# GitHub token for GitHub Models API (https://models.github.ai)
# For local development: no config needed — token is auto-detected from `gh auth login`
# For deployed environments: set GITHUB_TOKEN to a valid GitHub token with Models access
# GITHUB_TOKEN=your-github-token-here
